plugins {
    id 'com.android.library'
}

android {
    namespace 'com.example.module'
    compileSdk 34

    defaultConfig {
        minSdk 21

        externalNativeBuild {
            ndkBuild {
                // Build flags, arguments, or abiFilters go here
            }
        }
    }

    externalNativeBuild {
        ndkBuild {
            path file('src/main/jni/Android.mk')
        }
    }
}
